# M2 拍谱识谱与逐句讲解 —— v0.3.0

- 状态：⬜ 未开始
- 目标：把"图片谱 → 结构化 TabDocument → 可渲染、可讲解、可回放"链路做完整：三通道识谱互补（拍照/文本/Guitar Pro），逐句大白话讲解（路线 B）+ 自动指法 DP 落地，形成"看谱 → 学谱 → 弹谱"闭环

## 范围与状态

| 特性 | 内容 | 状态 |
|---|---|---|
| F201 | 拍照/选图识谱管线：CameraX 拍照或相册选图 → FrameCodec 压缩 → LlmTabExtractor（视觉链）→ 合法性过滤 → TabDocument | ✅ 代码完成（2026-09-16，TakePicture+Photo Picker、讲解流式对话框；CameraX 相机预览页后置，先用系统相机），待真机验收 |
| F202 | 谱面渲染与点按试听 | ✅ 代码完成（2026-09-16，**自绘 Canvas 版**：TabLayout 几何 + TonePlayer 单音合成，midiToFreq 同源保证点按音高与换算一致）；alphaTab 精渲染为升级路径（引入需改 gradle 配置，待用户确认），待真机验收 |
| F203 | 识别结果编辑修正：手动改弦/品/小节后保存回 TabDocument（识别与讲解分离的兜底） | ✅ 代码完成（2026-09-16，TabBarEditDialog 逐行编辑 + makeNote 边界校验 + withBar 回写；讲解/逐句讲解/指法参考均基于修正后数据），待真机验收 |
| F204 | 拍谱讲解（基于结构化数据）：基于 TabDocument + 谱图，输出按弦/拨弦/技巧说明与练习建议 | ✅ 代码完成（2026-09-16，explainTabDocument 走快答链流式、temperature 0.3 保一致；不再重新看图），待真机验收 |
| F205 | Guitar Pro 导入：alphaTab 导入 .gp3~.gp8 → TabDocument（唯一谱面数据模型） | ⛔ 阻塞：引入 alphaTab 需改 gradle 配置（CLAUDE.md：需用户确认）；确认后即开工 |
| F206 | 移调/变调夹计算器：原调→新调换算、变调夹夹几品换指法（tonal.js/teoria.js 思路移植 Kotlin） | ✅ done（2026-09-16，CI 绿；core/music 纯 JVM + 乐理页对话框） |
| F207 | 逐句大白话讲解管线（图片谱，调研定稿 2026-09-15）：路线 B 识谱→TabDocument→全曲乐句分段→逐句生成（总述/左手/右手/术语大白话/难点/自查）；CoVe 覆盖审计防漏音（剥离文本仅凭 TabDocument 重列音符清单比对，不一致重写） | ✅ 代码完成（2026-09-16，PhraseCoach 管线 + PhraseSegmenter/CoverageAuditor/PhraseExplainCodec 全 JVM 单测 + mock 管线测试），待真机验收 |
| F208 | 乐句卡片式点读 UI：2-4 小节/句卡片流，配小节序号与原图对照；音符级点读兜底；整段系统 TTS 朗读；讲解本地缓存可离线回看 | ✅ 代码完成（2026-09-16，PhraseReportDialog 卡片流 + TtsController + PhraseCache 内容寻址缓存），待真机验收 |
| F209 | 自动指法分配（DP）：自研动态规划 弦/品序列→手指编号(0-4)与把位建议，代价=换弦/跨把位/伸展/同把位优先；LLM 指法必须过规则校验 | ✅ done（2026-09-16，CI 绿；FingeringSolver 引擎 19 用例 + 集成进 F207 提示词参考与规则拦截） |

## 验收标准

- 对 3 种谱源（书页/截图/打印谱）识谱可用率达标（§9 M2）；可重拍重试；渲染与源谱一致、点按试听音高与 midi() 一致
- 讲解基于结构化数据：同一段谱两次讲解结论一致（不重新看图重讲）；能指导实际按弦；≥20 音样例谱覆盖审计 0 漏音 0 重复、每句含 finger 字段、术语首现带一句解释
- Guitar Pro 导入 3 个不同版本抽查一致；移调/变调夹 C→G 上移 5 品等 ≥10 组用例全对（单元测试）
- 指法 DP：爬格子/C-Am-G-F 和弦进行等 ≥10 组用例符合常识，非法指法被规则校验拦截（单元测试）

## 决策与备注

- 识别与讲解分离：识谱只产出可校验/可回放/可编辑的 TabDocument，讲解基于结构化数据——避免"每次问都重新看图、每次讲得不一样"（§8.1 设计要点）
- 逐句讲解走"结构化优先"路线 B，不做模型直接看图即席讲解：防漏音可程序化审计、图只编码一次、点哪讲哪依赖结构化索引（§8.7）
- 切分粒度三层：段落级概览 → 乐句级讲解（默认单元，2-4 小节）→ 音符级点读兜底；固定结构：总述→左手→右手→术语大白话→难点→自查（§8.7）
- 防漏音 CoVe 覆盖审计：讲解后剥离文本、仅凭 TabDocument 重列音符清单做集合比对，不一致则重写（§8.7）
- 自动指法自研 DP（换弦/跨把位/伸展代价，参考 MIT 6.006 与 arXiv:2407.09052）；不存在拿来即用的开源指法库（§8.7）——落地时横按按物理口径建模：同品异弦同指允许当合法横按（跨度内无更低品），否则 F 和弦无解；非空弦音 ≤4 的"偷懒小横按"加代价溢价保住惯用指形
- 交互分期：本里程碑 = Phase 1 乐句卡片式点读 + 整段 TTS 朗读；逐音高亮跟播（Phase 2）、先讲后练闭环（Phase 3）后置（§8.7）
- F202 路线说明（2026-09-16）：alphaTab 1.8.4 引入因 gradle 配置确认门槛暂缓，按决策点备选自绘 Canvas 落地（验收标准全覆盖）；alphaTab 仍为后续升级路径
